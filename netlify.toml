[build]
  publish = "dist/client"
  command = "npm run build"

[functions]
  directory = "dist/server"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server"
  status = 200

[build.environment]
  NODE_VERSION = "18"
